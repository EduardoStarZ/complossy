# Installing and running

Run `install.bat` to install ffmpeg (or do it manually with `winget install ffmpeg`, and run the executable file to run complossy.
The generated compressed images will always be created under the same directory where the original image lies. Running the program is
very intuitive.
